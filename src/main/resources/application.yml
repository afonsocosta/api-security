spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sc_security?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect
  application:
    name: api-security